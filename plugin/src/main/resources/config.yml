debug-server:
  host: localhost
  port: 2525
  reconnect-delay-seconds: 5
  # Timeout before auto-resuming a paused function (safety net)
  breakpoint-timeout-seconds: 30

logging:
  # Log every function call and command to console (verbose)
  trace-all: false
